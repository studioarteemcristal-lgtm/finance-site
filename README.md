# Finance Site

Servidor Node + SQLite com frontend estático.

## Start local
1. `npm install`
2. `npm start`
3. Acesse `http://localhost:3000`

Usuário padrão:
- usuário: `leilaine`
- senha: `Bn@75406320`
